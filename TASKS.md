# Edge Workspace Manager - 任务与计划

## 已完成功能

### 标签页批量移动（一对多 / 多对一）

- **数据层**：在 `background.js` / `js/dataStore.js` 中新增 `moveTabsToWorkspace` 与 `moveTabsToWorkspaces` API，支持：
  - 同一源工作区批量移动到同一目标工作区；
  - 同一源工作区分别移动到多个目标工作区（一对多）；
  - 多个源工作区移动到同一目标工作区（多对一）。
- **消息层**：`background.js` 新增 `MOVE_TABS` 消息处理器。
- **UI 层**：`popup.js` 标签页项增加多选复选框，选中后拖拽即可批量移动到目标工作区；`popup.css` 增加选中态样式。
- **测试**：`tests/test.js` 新增测试 30-32，覆盖批量移动、一对多、多对一场景。

### 跨工作区移动稳定性

- 引入 `pendingCleanup` 记录待清理 URL 及目标工作区 ID。
- 优化窗口关联匹配算法，避免源工作区抢占目标窗口。
- `syncWorkspaceToWindow` 支持同 URL 多标签页复用，避免重复创建。

## 待实现功能

### 多对多（工作区）标签页移动

**目标**：支持一次操作中将多个源工作区的多个标签页同时移动到多个目标工作区，并在 UI 层面提供直观的映射方式。

**计划步骤**：

1. **UI 设计**：在 popup 中增加“批量移动”模式入口，允许用户：
   - 选择一个或多个源工作区；
   - 在源工作区中选择若干标签页；
   - 为每个选中的标签页指定目标工作区（例如通过下拉选择或拖拽到不同目标卡片）。
2. **数据层**：复用 `moveTabsToWorkspaces` API，前端生成完整的 moves 数组后一次性提交。
3. **交互优化**：
   - 提供“全选/取消全选”按钮；
   - 支持 Shift/Ctrl 连续多选；
   - 在拖拽过程中显示批量移动数量提示。
4. **测试补充**：增加端到端风格的单元测试，模拟多源多目标场景并验证：
   - 各源工作区正确关闭已移出标签页；
   - 各目标工作区正确创建移入标签页；
   - 无重复创建、无关联抢占。

**优先级**：中
**预计依赖**：当前批量移动 API 已为该功能奠定数据层基础。
